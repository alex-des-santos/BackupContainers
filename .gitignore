# Backups gerados pela Action (histórico ativo no Git)
# backups/

# Chaves e ficheiros sensíveis
*.pem
id_*

# Config local (se copiar config.example para config)
config

# Testes e specs (desenvolvimento)
tests/
.kiro/
